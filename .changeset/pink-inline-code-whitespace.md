---
'@loom-js/pink': patch
---

Fix `PinkInlineCode` rendering template whitespace around its children (pink styles `code` as `white-space: pre-wrap`), which showed up as padding and line breaks around inline code.
