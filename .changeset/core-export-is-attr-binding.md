---
'@loom-js/core': minor
---

Export `isAttrBinding` — the runtime guard for `activity.bind()` values — so consumers (design systems, props transformers) can pass a binding through to an attribute slot instead of stringifying it.
