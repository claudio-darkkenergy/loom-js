---
'@loom-js/pink': patch
---

Republish the element-syntax rewrite as 1.0.1. Version 1.0.0 was burned by an accidental publish in May 2024 (the registry still holds that stale artifact, and npm never allows a version number to be reused), so the 1.0.0 release commit could not reach npm — 1.0.1 is the first publishable version of the rewritten package. All breaking changes are documented in the 1.0.0 changelog entry; this release adds nothing on top.
