---
'@loom-js/pink': minor
---

Add `PinkInlineCode` — an inline code element (`<code class="inline-code">`) over the upstream appwrite/pink `.inline-code` styles, for highlighting short code snippets within a sentence.
