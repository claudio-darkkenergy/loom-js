# Tasks — lazy-content-rename

## 1. Core

- [ ] 1.1 Rename in `lazy-import.ts` (`lazyContent` canonical; `importLazy` deprecated alias per D2); migrate `tests/types/lazy-import.types.ts`; suite + type-checks green
- [ ] 1.2 **Minor** changeset with the one-line migration

## 2. Docs

- [ ] 2.1 Topic 09 + README: heading `lazyContent`, opening differentiator (one machinery, two entry points), bridge sentence updated, example renamed; map outline touch; draft re-push
