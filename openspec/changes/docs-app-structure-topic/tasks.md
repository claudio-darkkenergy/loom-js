# Tasks — docs-app-structure-topic

## 1. Gates

- [ ] 1.1 Confirm `server-first-loom-app` landed and the build-package question (docs-build-tool D2) is resolved; note the grouped-nav outcome for placement

## 2. Outline (review gate)

- [ ] 2.1 Write the outline with `apps/loom` source pointers (D1/D2); settle standalone-vs-paired with build-tool; maintainer reviews — no entry work before

## 3. Content & publish

- [ ] 3.1 Author, push draft, review; add the Server Rendering topic's pointer; create/list the entry per the nav shape; publish and verify against the coverage spec
