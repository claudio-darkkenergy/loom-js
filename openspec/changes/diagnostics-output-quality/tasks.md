# Tasks — diagnostics-output-quality

## 1. Contract (TDD)

- [ ] 1.1 Red: formatter specs — segment anatomy as argument arrays, capability fallback to plain segments, attribution preserved (bound-method identity), label + generated-tag resolution, pending enumeration with cap
- [ ] 1.2 Green: formatter in `loom-console` land; `ActivityOptions.label`; settlement subject refs (D3, cost-measured); migrate existing call sites to the anatomy

## 2. Message pass

- [ ] 2.1 Sweep every always-on warning for the remedy clause (D4); sweep narration call sites onto the anatomy; before/after console screenshots for review

## 3. Docs & coordination

- [ ] 3.1 README diagnostics section + topic 13: the anatomy, `label`, the enumeration; draft re-push
- [ ] 3.2 Note the contract by name in `activity-transform-concurrency` and `component-instance-state` (their notices adopt it at apply)
- [ ] 3.3 **Minor** changeset
