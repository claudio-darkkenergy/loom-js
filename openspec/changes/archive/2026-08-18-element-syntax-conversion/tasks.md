## 0. Prerequisites

- [x] 0.1 Baselines at apply time: full core/pink/app suites and type-checks green; measure pink dist and app bundle (min+gzip); build the pre-conversion app snapshot for the parity harness. Re-verify the tree (the `fix-docs-toc-duplication` WIP may have landed or moved). → WIP landed + archived (`c427e40`); core 169 green; pink type-check green; **app `type-check` has 8 pre-existing error lines at HEAD** (snapshotted to scratchpad; conversion gate is "no new errors"). Pink dist **3,328 B** min+gzip; app bundle **34,246 B** (all build JS, gzip -9); pre-conversion snapshot saved. Note: pink's `exports.import` is `./src/index.ts`, so the app bundles pink from source — app bundle is the consumer metric.
- [x] 0.2 Audit-rule check (`SOLID-AUDIT-REPORT.md`) for the pink/app files this change touches — resolve or note per severity rules. → No 🔴 in scope. Binding 🟡s: `pink-button` LSP (resolve in 2.1 via the entry's document-the-contract option) and `docs/layout` SRP (resolve in 3.2 via `useDocsLayout()`). Opportunistic 🟢s: `pink/types` `is: Component | any` (2.5 narrows it) and `pink-code-panel-content` line-split extraction (2.3).
- [x] 0.3 Stand up context-count instrumentation (temporary local core patch per design Decision 2) and record the pre-conversion count on the app root page. → Counter in `contextFunction`'s fresh-context branch; **pre-conversion count: 106** contexts on `/` (zero page errors). Patch reverted, pristine builds restored; harness kept in scratchpad for the post-measurement.

## 1. Pilot (design Decision 2)

- [x] 1.1 Convert `PinkBox` (trivial wrapper consumer); DOM parity; stories updated. → template component; parity via the new fixture A/B harness (sorted-attr serializer per the named-slots precedent; whitespace-run/tag-boundary normalization for non-`pre` content). Story compiles unchanged.
- [x] 1.2 Convert `PinkContainer` + its app consumers (`HeroBanner`, `SecondaryContainer`, `PageLayout`'s `is=${Footer}` → `el('footer')`); DOM parity. → **Design amended (Decision 1): pure delegators stay functional** — the template form tripped the array limitation on `HeroBanner`'s children array (caught by the harness as `[object Text]…`). Landed as `SimpleComponent` over `is = el('div')` passing the flat tags shape; `el()` widened to accept flat `id`/`style` (delta spec on `core-element-components`) because tags' `mergeAllowedAttrs` waterfall clobbers `attrs.style` when only flat `className` is set. Pulled the 🟢 `PinkDynamicProps` ISP fix forward (index signature removed, `is: Component`) — ripple fixes in `pink-tag`/`pink-interactive-tag`/story typing; core `PossibleAttrs` index widened to admit `StyleProp` (runtime already supported it). `HeroBanner`'s stray `role` → `attrs.role`. Whole-app `#layout`: **byte-equal, zero page errors**; fixtures all OK; 169 core tests green; 0 new app type errors.
- [x] 1.3 Convert `PinkTopNav` (list-heavy; `items=` stays data-shaped, the `Li`/`Link` loop becomes authored markup/`RouteLink` where sensible); DOM parity. → template component + nested `TopNavItem` template (both component-tag-free, so item arrays take the proven plain-template path); local `PinkTopNavItemProps` replaces the tags `LinkProps` dependence; per-item `onClick` still overrides the nav-level handler; `target` keeps the `_self` default for parity. `RouteLink` adoption deferred to the app conversion (the `items` data API keeps handler-based routing). Fixtures OK; whole-app `#layout` byte-equal.
- [x] 1.4 Record pilot metrics: pink dist, app bundle, context count before/after. If any pattern is falsified, pause and amend design before the sweep. → **Context count 106 → 104** (net −2 from three components: TopNav went 5 wrapper contexts → 3 template contexts; the delegator stayed at zero). Pink dist 3,328 → 3,537 B (+209 B — pink now carries its own templates instead of leaning on tags' shared machinery). App bundle 34,246 → 34,651 B (+405 B gzip, including the core `el()` widening; net payoff arrives when tags' 1,595 B leaves at retirement). **One pattern falsified and amended** (Decision 1 amendment: pure delegators stay functional); everything else held. Sweep may proceed on the amended patterns.

## 2. Pink conversion (leaf → consumer order, parity per component)

- [x] 2.1 `elements/` leaves: `pink-inline-tag`, `pink-loader`, `pink-status`, `pink-card`, `pink-tag`, `pink-interactive-tag` (`href ? el('a') : el('button')`), `pink-boxes`, `pink-buttons-list`, `pink-tooltip`, `pink-button`. → All ten converted on the amended patterns: templates for structure-owners (`inline-tag`, `loader`, `status`, `boxes`, `buttons-list` + nested `ButtonsListItem` li template), functional delegators over `el()` for `is`-shaped roots (`card`, `tag`, `interactive-tag`, `tooltip`, `button`). **`el()` widened once more: `onClick` → `$click`** (red-green, suite now 170; delta-spec scenario updated) — tags roots bound `$click` and real call sites forward `onClick`. Tags root defaults reproduced via `attrs` (`type="button"`; `href` + `target="_self"`); on `PinkButton` the button-only props (`disabled`/`title`/`type`/`aria`) are documented as root-scoped, resolving the **LSP 🟡 → ✅** (document-the-contract; props now local — tags `ButtonProps` dependence gone); the pilot's `pink/types` ISP 🟢 also marked ✅. API deltas: `PinkInteractiveTag` gains typed `disabled`; `TagProps` drops the pilot's flat `href` shim (attrs-routed). **Found:** a style value resolving to nothing renders the `⚡` sentinel through the `style=` binding (the `$attrs` path drops it) — worked around in `PinkButton` (conditional style); candidate core fix for a follow-on change. Element stories' tags scaffolding → `el()`. Parity: 27/27 fixtures byte-equal; whole-app `#layout` byte-equal, zero page errors; all type-checks + prettier clean; app baseline 8 unchanged.
- [x] 2.2 `modifiers/`: `with-icon`, `with-tooltip` → `el('span')` (the transformer contract stays props-in/props-out). → Straight swap: `Icon` and the tooltip popup build via `el('span')`; `iconProps` retyped from tags `SpanProps` to core `ComponentInputProps` (same shape). Contract untouched otherwise (the `WithIconProps` index signature stays — revisit at 2.5 if the pink-wide types pass wants it gone). Parity via the consuming-component fixtures (`PinkTag`/`PinkButton`/`PinkTooltip` icon + popup spans): 27/27 byte-equal; pink type-check clean. Whole-app re-check deferred to 2.5 — the working tree now carries the parallel `fix-per-render-hook-leaks` app WIP, so a clean app A/B isn't currently buildable from the tree.
- [x] 2.3 `components/`: `pink-action-bar`, `pink-avatar`, `pink-avatar-group`, `pink-code-panel` (+content/header), `pink-drop-list`, `pink-tabs`, `pink-toggle-button`. → Templates: `action-bar` (section + both region divs), `avatar-group`/`drop-list`/`tabs`/`toggle-button` (list hosts + per-item li templates; item anchors/buttons via `el('a')`/`el('button')` + `withIcon` in the map — value position), `code-panel-header`, `code-panel-content` (+ `CodeLine`'s inner span → markup). Functional delegators: `avatar` (branching `img`/`div` root; `Img`'s `height`/`width` `auto` defaults and attrs-routed `alt`/`src` preserved), `code-panel` (`is = el('span')` + the `PinkButton`-style empty-style guard), `DropSection`, `TabsButtonScroll`. `PinkDropList` wrapper is a template with `.List`/`.Section` statics kept via `Object.assign`. Link-root parity: `href` omitted when absent (matching tags' empty-string drop), `target="_self"` default; button roots emit `type="button"` — all via the established conventions. **Resolved the `pink-code-panel-content` SRP 🟢 → ✅** (`splitCodeLines` extracted). Types localized off tags (`DropListItemProps`, `LinkItemProps`, toggle-button item props — narrowing surfaced and fixed a latent `PinkColor | string` in the avatar-group story). Harness upgraded: whitespace inside `<pre>` is now always compared verbatim (non-exact mode collapses only outside `<pre>`). Stories' tags scaffolding (`Span`) → `el('span')`. Parity: 41/41 fixtures byte-equal (14 new for 2.3); pink type-check + prettier clean.
- [x] 2.4 `layout/`: `pink-grid-box`, `pink-grid-item`, `pink-side-nav`. → **Scope grew by `pink-grid-header`** (not on the task list, but its template still rooted on `<${Header}>` — now a plain `<header>` template; doc comment updated). `grid-box`: delegator over `is = el('ul')` with the empty-style guard (fifth style-vars component). `grid-item`: stays functional over `PinkCard` — a `<${PinkCard}>` component tag with the corner-div array child is exactly the forbidden array shape — corners/wrapper via `el('div')`, keys kept. `side-nav`: `SideNavTop`/`SideNavBottom` become templates; root stays `is = el('nav')` delegation with the level-1 wrapper as an `el('div')` value. **Breaking (for the changeset): `PinkGridBox` no longer forwards `item`/`itemProps` to a tags `Ul` root — callers author their items** (its story now builds `el('li')`-wrapped cards; also converted the remaining story scaffolding: side-nav's `Link` → `el('a')` + attrs href, grid-header's `H2`/`Div`/`Span`, container's `Paragraph`). Parity 48/48 fixtures byte-equal (7 new); **`packages/pink/src` now has zero `@loom-js/tags` imports** — 2.5's import sweep is already satisfied, leaving types/storybook/metrics. Pink type-check + prettier clean.
- [x] 2.5 Pink-wide: `is` prop type moves off tags types to core `Component`; storybook builds clean; pink dist re-measured. → `is: Component` landed in the pilot; the sweep localized every remaining item/link/button prop type — zero tags imports (and zero tags type references) across `packages/pink/src`, verified. Storybook production build clean. **Whole-app gate (deferred from 2.2) caught a real regression the fixtures missed:** the home page's `SyntaxCards` used `PinkGridBox`'s retired `item`/`itemProps` passthrough — post rendered an empty `<ul>`. Fixed by pulling that 3.1 slice forward (authored `el('li')`-wrapped `ContentCard`s); `#layout` then **byte-equal (3,434 B), zero page errors**, A/B isolating exactly pink 2.2–2.4 (`Features.ts` renders via tags `Ul` directly, unaffected until 3.1). Metrics: pink dist 3,537 → **4,222 B** min+gzip (+685 B — pink owns all its template machinery now; payoff lands when tags' 1,595 B leaves the app at retirement). App bundle 36,349 B vs 35,398 B rebuilt-pre on identical app code → **pink-sweep delta +951 B gzip** (the raw 34,651 → 36,349 span includes the parallel `fix-per-render-hook-leaks` landing, +747 B, not this change). App type-check baseline moved 8 → **3 error lines** (hook-leaks fixes); still zero new from this change. Core 170 green; prettier clean.

## 3. App conversion (16 files, coordinate with any open WIP)

- [x] 3.1 Components: `BrandLogoLink` (→ `RouteLink`), `ContentCard`, `FocalContainer`, `HeroBanner`, `SecondaryContainer`, `Breadcrumbs`, `SkeletonLoader`, `Toc`, `TopicContent`, `FeatureCard`, `Features`, `ResponsiveImage` (local → core `Picture`), `StyledRichText` (`renderNode` → `el()`), `ContentfulRichText` (local `mergeAllowedAttrs` copy). → Templates: `ContentCard` (root `<${PinkCard}>` — its consumers wrap it as a single li child, never an array item), `Breadcrumbs` (+li item template), `SkeletonLoader` (bones map as `el('div')` values), `Toc` (+link-item template). Functional (children-forwarders and array-consumed components per the Decision 1 amendment): `FeatureCard`, `SecondaryContainer`, `FocalContainer`, `HeroBanner` — **HeroBanner's template attempt tripped the array limitation live** (component-tag-rooted template ⇒ fragment; the home page holds it in a children array; whole-app gate caught the `[object Text]` stringify) — reverted to functional. **Corollary sharpened: a component consumed as a children-array item must not be a component-tag-rooted template.** Also: `...${rest}` spreads of a template's own props into component tags forward lifecycle base props (`createRef`/`node`/…) and fail typing — dropped in favor of explicit surfaces. `RouteLink` gets explicit `target: '_self'` for byte parity with tags `Link`'s default; converted image call sites pin tags `Img`'s `height`/`width` `'auto'` defaults; `ResponsiveImage` deleted outright (zero callers; future consumers import core `Picture` directly — the `components/media/` dir went with it; `FeatureCard` also has no callers but is kept, conversion-only). `mergeAllowedAttrs` copied next to `ContentfulRichText` (its one consumer).
- [x] 3.2 Pages: `docs/layout.ts`, `docs/index.ts` (effect → `el('aside')`), `pages/layout.ts` leftovers (`Svg` import moves to core). → All three converted (docs layout's `Div` root → `el('div')` atop the landed `useDocsLayout()` refactor); contentful `types.ts` drops tags `LinkProps` for a local `NavLinkProps`. **Zero `@loom-js/tags` imports remain in `apps/loom/src`.**
- [x] 3.3 Whole-app DOM parity on `#layout` plus one content-bearing docs route; app bundle re-measured. → Both builds re-made with dotenvx so `__API_URL__` bakes (plain `pnpm -F build` leaves it empty and the docs route can't fetch); `/` byte-equal **3,434 B** and `/docs/get-started` byte-equal **14,060 B** against live API data, zero page errors both sides — the docs capture exercises Breadcrumbs, SkeletonLoader, Toc/TopicToc, TopicContent, StyledRichText, ContentfulRichText, DocContainer, DocsSideNav. App bundle 36,393 → **37,339 B** gzip (+946 B for the app sweep): the shared chunk shrank 1,010 B as tags tree-shook out, but new templates + core `Picture`/`RouteLink`/`Svg` and a reshuffled code-split cost more — full metrics story lands in 5.4. Caveat: the post-side build compiled core from a tree carrying the parallel `add-reactive-unsubscribe` WIP (+56/−11 lines), so the +946 B may include a small share of it — 5.4 re-measures on a clean tree. App type-check at the 3-line baseline (zero new).

## 4. Tags retirement (design Decision 3)

- [x] 4.1 Zero-import check (`grep` sweep incl. stories); remove `@loom-js/tags` from pink `peerDependencies` and any remaining manifests. → **The sweep found scope the audit missed: three in-workspace `lib/` files** — `lib/contentful`'s `default-renderers.ts` (all 24 renderers → `el()`; tags code had still been reaching the app bundle through `documentToComponents`) and `types.ts` (`LinkProps` → local `NavLinkProps`), plus `lib/storybook`'s `RenderVariants` (tags `Ul` render-prop → authored `el('ul')`/`el('li')`; its now-parameterized `itemProps` typing exposed and fixed a mistyped `pink-inline-tag` story variant). Also `apps/examples/activity-effect-nested-array` (in the workspace via the `apps/**/*` glob, contrary to the docs' "only apps/loom" claim) — converted to `el()`. Manifests: tags removed from pink (`dependencies` — not `peerDependencies` as documented — and dev/peer), `lib/contentful`, `lib/storybook`, `apps/loom`, and the example; pink `rollup.config.ts` external pruned. Excluded-from-workspace scratch apps (`apps/docs`, `apps/sandbox`) keep their imports — outside the install graph, per the spec's first-party scope.
- [x] 4.2 Delete `packages/tags`; prune `.prettierrc` `packageJSONFiles` entry and `CLAUDE.md` references; `pnpm install` + full builds green. → Deleted — provenance note: the staged deletion was swept into the parallel session's `531d0de` (`feat(core): automatic unmount teardown`) via the shared git index, so the 62 file deletions live in that commit rather than this change's `77e8392` (which carries the manifests/config); `.prettierrc` entry, `CLAUDE.md` (repo shape + package list), and `.claude/skills/skill-config.md` (stack row, folder rows, output/config mentions) pruned. `pnpm install` clean; core 170 green; all package type-checks 0; app at the 3-line baseline; storybook production build clean; prettier clean. Parity after the lib renderer conversion: `/` byte-equal 3,434 B and `/docs/get-started` byte-equal **14,060 B** against live content. **App bundle 37,339 → 35,688 B** (−1,651 B — the tags code riding in via `lib/contentful` is finally gone); net for the whole conversion so far: 34,246 → 35,688 B (+1,442 B, full story in 5.4).
- [x] 4.3 Owner step: `npm deprecate '@loom-js/tags'` with the message pointing to element syntax + core survivors (exact text drafted in the changeset). → Run 2026-08-18 after account recovery, against all 9 published versions (0.0.10–0.0.18, 1.0.0); registry verified. Release context: the conversion shipped as `@loom-js/core@0.6.0` + `@loom-js/pink@0.2.0` (not 1.0.0 — that number was burned by an accidental May 2024 publish, now deprecated too; pink deliberately stays pre-1.0, breaking changes on minors), published via npm trusted publishing (OIDC) with provenance.

## 5. Docs & release

- [x] 5.1 `packages/core/README.md`: element syntax leads; functional form documented as architecture/escape hatch; tags references gone. → README already led with element syntax and had zero tags references post-prerequisites; added the explicit **"element syntax is the primary authoring surface"** positioning paragraph (functional = the architecture the syntax compiles to + the sanctioned value-position escape hatch) plus the two composition constraints the conversion proved out (regions interpolate, never as array items; array-consumed components stay off component-tag roots).
- [x] 5.2 `.claude/skills/skill-config.md`: authoring convention updated (markup-first, value-position escape hatch, `el()`). → Added under Component model, including the conversion-pattern rules (structure-owners → templates; delegators functional over `el()`; the fragment-root/array constraint; the empty-style omission rule).
- [x] 5.3 Changesets: pink **major** (composition-API break, peerDep removal, accumulated per-component notes). → `element-syntax-conversion-pink.md` (major: tags retirement, `PinkDynamicProps` narrowing, `PinkGridBox` item passthrough removal, `PinkButton` local props + root contract, list hosts, type localization; **the 4.3 `npm deprecate` command text included verbatim**) + `el-flat-surface-widening.md` (core minor: flat `id`/`style`, `onClick`, `PossibleAttrs` `StyleProp`). `changeset status`: core minor + pink major ✓.
- [x] 5.4 Full verification: all suites, type-checks, prettier `--check`, storybook build; final metrics summary recorded in design. → Core 170 green + test type-check; core/pink/contentful/storybook/utils all 0 errors; app at the 3-line baseline; storybook production build clean; prettier clean. **Final context count on `/`: 106 → 86 (−19%)** via the re-applied temporary instrumentation (reverted; pristine app rebuilt). Metrics table + narrative recorded in design.md.
- [x] 5.5 Update `element-syntax-first` umbrella tasks (sections 3–4) and note the umbrella spec-prune for its eventual archive. → Umbrella 3.1–3.3 and 4.1–4.3 checked with pointers here; archive note added: prune the umbrella's `specs/element-syntax-authoring/` draft at archive time (superseded by this change's delta spec).
