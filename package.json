{
  "private": true,
  "version": "0.0.10",
  "repository": {
    "type": "git",
    "url": "https://github.com/darkkenergy/loomjs"
  },
  "scripts": {
    "clean": "find ./ -name node_modules -type d -exec rm -rf {} +",
    "docs:start": "pnpm -F @loom-js/docs start",
    "update:all": "pnpm -r update -i -L"
  },
  "dependencies": {
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=16.16.0",
    "pnpm": ">=8.6.0"
  }
}
