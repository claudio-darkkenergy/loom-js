{
  "name": "loom-js",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/darkkenergy/loomjs"
  },
  "scripts": {
    "clean": "find ./ -name node_modules -type d -exec rm -rf {} +",
    "build": "turbo build",
    "build-packages": "turbo build-packages",
    "dev": "turbo dev",
    "publish-packages": "pnpm build-packages && changeset version && changeset publish",
    "update:all": "pnpm -r update -i -L"
  },
  "devDependencies": {
    "@loom-js/typescript-config": "workspace:*",
    "prettier": "^3.2.4",
    "prettier-plugin-sort-imports": "^1.8.3",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.14.1",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@changesets/cli": "^2.27.1"
  }
}
