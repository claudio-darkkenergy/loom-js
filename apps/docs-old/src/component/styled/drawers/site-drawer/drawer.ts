import { StandardDrawer } from '@app/component/simple';
import { Styled } from '@app/utils';

import styles from './styles.scss';

export const StyledSiteDrawer = Styled(StandardDrawer, styles.siteDrawer);
